[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ur5-flow-matching"
version = "0.1.0"
description = "Conditional flow matching for planar UR5 obstacle-avoiding trajectories"
requires-python = ">=3.10"
dependencies = ["numpy>=1.24", "torch>=2.1", "matplotlib>=3.7", "tqdm>=4.66"]

[project.optional-dependencies]
dev = ["pytest>=7.4"]

[tool.setuptools.packages.find]
where = ["src"]

